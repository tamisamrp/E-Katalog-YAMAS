<%- include('../partials/header/__header-dashboard.ejs', { title: 'Dashboard Manajer' }) %>
<%- include('../partials/nav-side/__nav-side-manajer.ejs') %>
<%- include('../partials/messages/__messages-dashboard.ejs') %>

<div class="main-panel">
    <div class="content-wrapper">

        <div class="row">
            <div class="col-12 col-sm-6 col-lg-4 grid-margin stretch-card">
                <div class="stat-card card w-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center justify-content-between">
                            <div>
                                <div class="stat-label">Buku Dihapus</div>
                                <div class="stat-number" data-count="<%= totalBukuHapus[0].total_buku_hapus %>"><%= totalBukuHapus[0].total_buku_hapus %></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-12 col-sm-6 col-lg-4 grid-margin stretch-card">
                <div class="stat-card card w-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center justify-content-between">
                            <div>
                                <div class="stat-label">Majalah Dihapus</div>
                                <div class="stat-number" data-count="<%= totalMajalahHapus[0].total_majalah %>"><%= totalMajalahHapus[0].total_majalah %></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-12 col-sm-6 col-lg-4 grid-margin stretch-card">
                <div class="stat-card card w-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center justify-content-between">
                            <div>
                                <div class="stat-label">Koran Dihapus</div>
                                <div class="stat-number" data-count="<%= totalKoranHapus[0].total %>"><%= totalKoranHapus[0].total %></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h4 class="card-title mb-0">Buku Terbaru</h4>
                            <a href="/manajer/buku" class="btn btn-primary btn-sm">
                                <i class="mdi mdi-eye"></i> Lihat Semua
                            </a>
                        </div>
                        <div>
                            <table class="table table-hover table-stacked">
                                <thead>
                                    <tr>
                                        <th>No</th>
                                        <th>Judul</th>
                                        <th>No. Klasifikasi</th>
                                        <th>Lokasi</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (newBukuHapus && newBukuHapus.length > 0) { %>
                                        <% for (let i = 0; i < newBukuHapus.length; i++) { %>
                                            <tr>
                                                <td data-label="No"><%= i + 1 %></td>
                                                <td data-label="Judul"><%= newBukuHapus[i].judul %></td>
                                                <td data-label="No. Klasifikasi"><%= newBukuHapus[i].no_klasifikasi %></td>
                                                <td data-label="Lokasi"><%= newBukuHapus[i].lokasi_buku %></td>
                                            </tr>
                                        <% } %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="4" class="text-center">Tidak ada data</td>
                                        </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h4 class="card-title mb-0">Majalah Terbaru</h4>
                            <a href="/manajer/majalah" class="btn btn-primary btn-sm">
                                <i class="mdi mdi-eye"></i> Lihat Semua
                            </a>
                        </div>
                        <div>
                            <table class="table table-hover table-stacked">
                                <thead>
                                    <tr>
                                        <th>No</th>
                                        <th>Judul</th>
                                        <th>Edisi</th>
                                        <th>No. Klasifikasi</th>
                                        <th>Lokasi</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (newMajalahHapus && newMajalahHapus.length > 0) { %>
                                        <% for (let i = 0; i < newMajalahHapus.length; i++) { %>
                                            <tr>
                                                <td data-label="No"><%= i + 1 %></td>
                                                <td data-label="Judul"><%= newMajalahHapus[i].judul %></td>
                                                <td data-label="Edisi"><%= newMajalahHapus[i].edisi %></td>
                                                <td data-label="No. Klasifikasi"><%= newMajalahHapus[i].no_klasifikasi %></td>
                                                <td data-label="Lokasi"><%= newMajalahHapus[i].lokasi_majalah %></td>
                                            </tr>
                                        <% } %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="5" class="text-center">Tidak ada data</td>
                                        </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h4 class="card-title mb-0">Koran Terhapus Terbaru</h4>
                            <a href="/manajer/koran" class="btn btn-primary btn-sm">
                                <i class="mdi mdi-eye"></i> Lihat Semua
                            </a>
                        </div>
                        <div>
                            <table class="table table-hover table-stacked">
                                <thead>
                                    <tr>
                                        <th>No</th>
                                        <th>Penerbit</th>
                                        <th>Tahun</th>
                                        <th>Bulan</th>
                                        <th>Dihapus Pada</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (newKoranHapus && newKoranHapus.length > 0) { %>
                                        <% for (let i = 0; i < newKoranHapus.length; i++) { %>
                                            <tr>
                                                <td data-label="No"><%= i + 1 %></td>
                                                <td data-label="Penerbit"><%= newKoranHapus[i].nama_penerbit || '-' %></td>
                                                <td data-label="Tahun"><%= newKoranHapus[i].tahun %></td>
                                                <td data-label="Bulan"><%= newKoranHapus[i].bulan %></td>
                                                <td data-label="Dihapus Pada"><%= new Date(newKoranHapus[i].dibuat_pada).toLocaleString('id-ID') %></td>
                                            </tr>
                                        <% } %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="5" class="text-center">Tidak ada data</td>
                                        </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/script/__script-dashboard.ejs') %>